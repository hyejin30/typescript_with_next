export { default as Button } from "./Button";
// export { default as Input } from './Input';

// as-is : import { Button } from 'atoms/Button'; import { Input } from 'atoms/Button';
// to-be : import { Button, Input } from 'atoms';
